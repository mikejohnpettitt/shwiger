<div class="banner full-height">
    <div class="banner-logo-container">
        <h1>shwiger</h1>
        <div class="banner-logo"></div>
    </div>
    <p>smart Chinese flashcards</p>
    <% if user_signed_in? %>
        <%= link_to user_decks_path, class: "button primary large" do %>
        My dashboard <i class="fa-solid fa-arrow-right"></i>
        <% end %>
    <% else %>
        <%= link_to demo_path, class: "button primary large" do %>
        Demo <i class="fa-solid fa-arrow-right"></i>
        <% end %>
    <% end %>
    <div class="deck-motif-banner">
    <% Card.where(deck:2).first(150).each do |card|%>
    <% card.entry.chinese.split("").each do |character| %>
    <% if character != "…" %>
    <div><%= character %></div>
    <% end %>
    <% end %>
    <% end %>
    </div>
</div>
